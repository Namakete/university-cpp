// Write a program that displays a series of Fibonacci numbers on the screen.
// where each number is the sum of the previous two (1, 2, 3, 5, 8,
// 13, 21, ...)

namespace magic
{
    /*
     * @brief The sequence of `Fibonacci` numbers 
     * is determined by the formula Fn = Fn-1 + Fn-2. 
     * That is, the next number is obtained as 
     * the sum of the previous two.
     * 
     * @file From `pow.hpp`.
     */
    int fibonacci(int value);
}
