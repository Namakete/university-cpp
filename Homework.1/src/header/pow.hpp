// Write a program that accepts two numbers `a` and `b` from the keyboard
// and deriving a to the power b. The degree must be given as an integer
// number and can be negative.

namespace math
{
    /*
     * @brief A recursive function to get `a ^ b`.
     *
     * @param `base` is value container whose values are
     * summed of `exponent` times.
     * 
     * @file From `pow.hpp`.
     */
    double pow(double base, int exponent);
}
